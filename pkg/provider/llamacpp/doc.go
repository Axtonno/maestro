// Package llamacpp provides the public construction facade for Maestro's
// llama.cpp provider adapter. Protocol details and DTOs remain internal.
// Optional local attestation binds a loopback server to an exact GGUF path,
// SHA-256, server build, model alias, and effective context window.
package llamacpp
