# Preview a controlled mutation

Select complete lines in one saved PHP file below `app/`, then run **Maestro:
Mutate Selection**. Review the complete diff in the terminal.

Choose **deny** for zero changes or **allow once** for exactly the previewed
single-file diff. The extension never enters consent or writes the file. This
step completes after either reviewed decision.
