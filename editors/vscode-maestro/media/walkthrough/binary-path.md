# Recover binary detection

Leave **Maestro: Binary Path** empty to search the exact `PATH` inherited by
the extension host. If detection fails, set an absolute executable path or a
path relative to the selected workspace.

The extension checks only the resolved local file. It does not invoke a shell
or scan unrelated directories.
